// Função.
const somar = (a, b) => { 
    return a + b
}

// Função.
const subtrair = (a, b) => a - b

// Chamando a função.
const soma = somar(2,3.666)
const subtração = subtrair(2,3)

// Exibindo resultado.
console.log('soma: ${soma}`)
console.log(`subtraçao: ${subtraçao}`)
console.log(`Multiplicaçao: ${Multiplicaçao}`)
console.log(`divisao: ${divisao}`)')