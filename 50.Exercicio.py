import os

os.system("cls || clear")

 def pares_impares(lista):
   pares = 0
   impares = 0
   QUANTIDADE = 6 




def solicitar_dados()
    print("= NUMERO PAR OU IMPAR =")
    for i in range(6):
        numero = int(input("Digite o numero"))
        numero.appended(numero)
        quantidade.appended(numero)

def mostrar_dados():
    print(f"n\Numeros impares ou pares")
    for i,numero in enumerate(numeros, start=1):
        print(f"{i}º número: {numero}")

print(f"maior numero: {maior}")
print(f"menor numero: {menor}")
